{
  "name": "Iodine",
  "description": "IPv4 over DNS tunnel",
  "command": "iodine",
  "categories": {
    "Options": {
      "options": [
        {
          "flag": "-f",
          "description": "Foreground mode"
        },
        {
          "flag": "-P",
          "description": "Password",
          "variable": true,
          "prompt_text": "Enter password:"
        }
      ]
    }
  },
  "required": {
    "nameserver": {
      "prompt": "Enter nameserver:",
      "description": "DNS server"
    },
    "topdomain": {
      "prompt": "Enter topdomain:",
      "description": "Top domain"
    }
  }
}